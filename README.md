# Serverless CSV Processing Pipeline (AWS)

A production-oriented reference implementation of a serverless batch ingestion pipeline:
**S3 → Lambda → SQS**.

This repository will include:
- Python processing logic (deterministic, defensive parsing)
- Terraform infrastructure (secure defaults)
- Tests, CI, and design docs